services:
  hummingbot:
    container_name: hummingbot
    # 使用你的 Docker Hub 镜像（包含 AMM_SWAP 和其他修改）
    image: firenirva/hummingbot:5ffe427e4  # ⚠️ 替换为你的实际用户名
    volumes:
      - ./conf:/home/hummingbot/conf
      - ./conf/connectors:/home/hummingbot/conf/connectors
      - ./conf/strategies:/home/hummingbot/conf/strategies
      - ./conf/controllers:/home/hummingbot/conf/controllers
      - ./conf/scripts:/home/hummingbot/conf/scripts
      - ./logs:/home/hummingbot/logs
      - ./data:/home/hummingbot/data
      - ./certs:/home/hummingbot/certs
      - ./scripts:/home/hummingbot/scripts
      - ./controllers:/home/hummingbot/controllers
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
    tty: true
    stdin_open: true
    network_mode: host
  #  environment:
  #    - CONFIG_PASSWORD=你的新密码
  #    - CONFIG_FILE_NAME=conf_amm_arb_GPS.yml

  gateway:
    restart: always
    container_name: gateway
    # 使用你的 Docker Hub 镜像（包含 gas fee 优化）
    image: firenirva/gateway:v2.13.0-custom  # ⚠️ 替换为你的实际用户名
    ports:
      - "15888:15888"
    volumes:
      - "./gateway-files/conf:/home/gateway/conf"
      - "./gateway-files/logs:/home/gateway/logs"
      - "./certs:/home/gateway/certs"
    environment:
      - GATEWAY_PASSPHRASE=admin
      - DEV=true
